// BoxType.cs — create as a new script file, no MonoBehaviour needed
public enum BoxType
{
    None,       // a "safe" box that should pass through the gate freely
    TypeA,
    TypeB,
    TypeC,
    TypeD
}